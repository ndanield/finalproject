/*
$("#roleChange").on("click", function () {
    $("#usersTable").load("public/js/divTempfile.txt", function (responseTxt,statusTxt,xhr ) {

        if(statusTxt === "success")
            console.log("Recargada exitosamente!");
        if(statusTxt === "error")
            console.log("Error: " + xhr.status + ": " + xhr.statusText)
    })
});*/
